[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v
pythonpath = src

markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require LocalStack / Docker)
